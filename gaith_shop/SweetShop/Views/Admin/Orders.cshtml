@model IEnumerable<Order>
@{
    ViewData["Title"] = "إدارة الطلبات";
    Layout = "_AdminLayout";
    var statusColors = new Dictionary<string, string>
            {
                ["Pending"] = "badge-pending",
                ["Processing"] = "badge-process",
                ["Shipped"] = "badge-done", // Or create a new class
                ["Delivered"] = "badge-done"
            };
    var pending = Model.Count(o => o.Status == "Pending");
    var processing = Model.Count(o => o.Status == "Processing");
    var shipped = Model.Count(o => o.Status == "Shipped");
    var delivered = Model.Count(o => o.Status == "Delivered");
}

<!-- Stats -->
<div class="row g-3 mb-4">

    <div class="col-6 col-md-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#fef3c7;"><i class="bi bi-clock" style="color:#92400e;"></i></div>
            <div>
                <div class="stat-value">@pending</div>
                <div class="stat-label">معلق</div>
            </div>
        </div>
    </div>
    <div class="col-6 col-md-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#dbeafe;"><i class="bi bi-arrow-repeat" style="color:#1e40af;"></i>
            </div>
            <div>
                <div class="stat-value">@processing</div>
                <div class="stat-label">قيد المعالجة</div>
            </div>
        </div>
    </div>
    <div class="col-6 col-md-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#d1fae5;"><i class="bi bi-box-seam" style="color:#065f46;"></i>
            </div>
            <div>
                <div class="stat-value">@shipped</div>
                <div class="stat-label">مشحون</div>
            </div>
        </div>
    </div>
    <div class="col-6 col-md-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#d1fae5;"><i class="bi bi-check-circle" style="color:#065f46;"></i>
            </div>
            <div>
                <div class="stat-value">@delivered</div>
                <div class="stat-label">تم التوصيل</div>
            </div>
        </div>
    </div>
</div>

<!-- Orders Table -->
<div class="admin-card">
    <div class="admin-card-header">
        <h5><i class="bi bi-bag-fill me-2" style="color:#1A8CB0;"></i>جميع الطلبات (@Model.Count())</h5>
    </div>
    <div class="admin-card-body p-0">
        @if (!Model.Any())
        {
            <div class="text-center py-5">
                <i class="bi bi-bag" style="font-size:48px;color:#cbd5e1;"></i>
                <p class="mt-3 text-muted">لا توجد طلبات بعد</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>رقم الطلب</th>
                            <th>العميل</th>
                            <th>الهاتف</th>
                            <th>المدينة</th>
                            <th>الإجمالي</th>
                            <th>تاريخ الطلب</th>
                            <th>الحالة</th>
                            <th>تحديث الحالة</th>
                            <th>تفاصيل</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in Model.OrderByDescending(o => o.OrderPlaced))
                        {
                            var badgeCss = statusColors.ContainsKey(order.Status) ? statusColors[order.Status] :
                            "badge-pending";
                            <tr>
                                <td style="font-weight:600;color:#1A8CB0;">#@order.Id</td>
                                <td style="font-weight:500;">@order.FirstName @order.LastName</td>
                                <td style="color:#64748b;font-size:13px;" dir="ltr">@order.PhoneNumber</td>
                                <td style="color:#64748b;font-size:13px;">@order.City</td>
                                <td style="font-weight:600;">@order.OrderTotal.ToString("0.00") د.أ</td>
                                <td style="font-size:13px;color:#64748b;">@order.OrderPlaced.ToString("yyyy/MM/dd")</td>
                                <td>
                                    @{
                                        string translatedStatus = order.Status switch
                                        {
                                            "Pending" => "معلق",
                                            "Processing" => "قيد المعالجة",
                                            "Shipped" => "مشحون",
                                            "Delivered" => "تم التوصيل",
                                            _ => order.Status
                                        };
                                    }
                                    <span class="status-badge @badgeCss">@translatedStatus</span>
                                </td>
                                <td>
                                    <form asp-action="UpdateOrderStatus" method="post"
                                        style="display:flex;gap:6px;align-items:center;">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@order.Id" />
                                        <select name="status" class="admin-form-control admin-form-select"
                                            style="padding:5px 8px;font-size:12px;min-width:130px;">
                                            <option value="Pending" selected="@(order.Status == "Pending")">⏳ معلق</option>
                                            <option value="Processing" selected="@(order.Status == "Processing")">🔄 قيد
                                                المعالجة</option>
                                            <option value="Shipped" selected="@(order.Status == "Shipped")">📦 مشحون</option>
                                            <option value="Delivered" selected="@(order.Status == "Delivered")">✅ تم التوصيل
                                            </option>
                                        </select>
                                        <button type="submit"
                                            style="padding:5px 10px;border-radius:7px;background:#1A8CB0;color:white;border:none;font-size:12px;cursor:pointer;">
                                            <i class="bi bi-check2"></i>
                                        </button>
                                    </form>
                                </td>
                                <td>
                                    <a asp-action="OrderDetails" asp-route-id="@order.Id" class="btn-admin-warning"
                                        style="font-size:12px;padding:5px 10px;">
                                        <i class="bi bi-eye-fill"></i> عرض
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
